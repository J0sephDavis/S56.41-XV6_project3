#include "user.h"
int main(void) {
	int *p = 0;
	printf(1, "hello [%x]\n", *p);
//	printf(1, "stack: %p\n", &p);
//	p = malloc(100);
//	printf(1, "heap: %p\n", p);
//	p = 0;
//	printf(0, "%c\n", p);
//	*p = 100;
//	free(p);
	exit();
}
